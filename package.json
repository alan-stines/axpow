{
  "name": "axpow-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate:biographies": "node ./scripts/generate-biographies.mjs",
    "dev": "npm run generate:biographies && vite",
    "build": "npm run generate:biographies && vite build --base=./",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.8",
    "marked": "^18.0.2"
  }
}
